<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fmohammadi.whatsapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.activitys.MainActivity"></activity>
        <activity android:name=".controller.activitys.ProfileActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity
            android:name=".controller.activitys.ChatActivity"
            android:parentActivityName=".controller.activitys.DashboaerdActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" /> <!-- optional (needed if default theme has no action bar) -->
        <activity android:name=".controller.activitys.SettingsActivity" />
        <activity android:name=".controller.activitys.DashboaerdActivity" />
        <activity android:name=".controller.activitys.LoginActivity" />
        <activity android:name=".controller.activitys.CreateAccountActivity" />
        <activity android:name=".controller.activitys.SplashScreanActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>